// A volume that REQUIRES another. Today the model resolves calls only inside a
// file, so this crosses nothing; when cross-module resolution lands it is the
// first place the volume gate should go red, and that redness is the point.
const alpha = require("./alpha");

function drive(x) {
    const out = alpha.run(x);
    return out;
}

module.exports = { drive };
