// A file this scanner cannot parse: decorators need a babel plugin that
// scanners/js_ast.ts does not enable. Carried with a `.txt` tail so tsc's
// `test/**/*.ts` include never tries to compile it — the same trick the
// call-graph fixtures use for their degenerate shapes.
class S { @log m() {} }
