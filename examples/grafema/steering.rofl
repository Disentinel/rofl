-- examples/grafema/steering.rofl — the human's steering, as facts with a tick.
-- Loaded under `vadim`. Every kill carries its reason; kills cascade to
-- obligations, never to facts.

-- @who vadim
report_every(5).
tick_closed(1). tick_closed(2). tick_closed(3). tick_closed(4). tick_closed(5).
