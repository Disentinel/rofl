-- facts/floor-census.rofl — GENERATED by scanners/floor_census.ts, do not edit.
--
-- What a store is made of, split by WHO CAN READ IT. The reasoning over
-- these rows is rules/floor-census.rofl; nothing here concludes anything.
-- Taken on node v24.13.0.

-- bucket_facts(Program, Bucket, N)   facts of that bucket
-- bucket_bytes(Program, Bucket, N)   sum of their factKey lengths
-- rel_facts(Program, Rel, N)         per reflection relation
-- store_facts(Program, N)            every fact in the store
-- store_bytes(Program, N)
-- refl_reader(File, Rel, Head, Sense)  a rule body in the tree that
--                                      reads a reflection relation
-- corpus_files(N) / corpus_unparsed(File)

-- ring1_unsealed: the same grammar with its sealed(rules) clause struck out
program(ring1_unsealed).  store_facts(ring1_unsealed, 2450).  store_bytes(ring1_unsealed, 122434).
  bucket_facts(ring1_unsealed, world, 91).  bucket_bytes(ring1_unsealed, world, 1812).
  bucket_facts(ring1_unsealed, executable, 675).  bucket_bytes(ring1_unsealed, executable, 54210).
  bucket_facts(ring1_unsealed, about_rule, 1435).  bucket_bytes(ring1_unsealed, about_rule, 51058).
  bucket_facts(ring1_unsealed, about_fact, 186).  bucket_bytes(ring1_unsealed, about_fact, 13533).
  bucket_facts(ring1_unsealed, provenance, 0).  bucket_bytes(ring1_unsealed, provenance, 0).
  bucket_facts(ring1_unsealed, kernel_table, 63).  bucket_bytes(ring1_unsealed, kernel_table, 1821).
  rel_facts(ring1_unsealed, asserted_by, 93).  rel_bytes(ring1_unsealed, asserted_by, 6720).
  rel_facts(ring1_unsealed, concludes, 140).  rel_bytes(ring1_unsealed, concludes, 4990).
  rel_facts(ring1_unsealed, conclusion_lit, 140).  rel_bytes(ring1_unsealed, conclusion_lit, 14215).
  rel_facts(ring1_unsealed, conclusion_tense, 140).  rel_bytes(ring1_unsealed, conclusion_tense, 5600).
  rel_facts(ring1_unsealed, has_conclusion, 140).  rel_bytes(ring1_unsealed, has_conclusion, 5040).
  rel_facts(ring1_unsealed, has_premise, 395).  rel_bytes(ring1_unsealed, has_premise, 13037).
  rel_facts(ring1_unsealed, in_perspective, 93).  rel_bytes(ring1_unsealed, in_perspective, 6813).
  rel_facts(ring1_unsealed, premise_lit, 395).  rel_bytes(ring1_unsealed, premise_lit, 36635).
  rel_facts(ring1_unsealed, premise_neg, 33).  rel_bytes(ring1_unsealed, premise_neg, 1317).
  rel_facts(ring1_unsealed, premise_pos, 266).  rel_bytes(ring1_unsealed, premise_pos, 9884).
  rel_facts(ring1_unsealed, reads_from, 140).  rel_bytes(ring1_unsealed, reads_from, 4900).
  rel_facts(ring1_unsealed, rule, 140).  rel_bytes(ring1_unsealed, rule, 3360).
  rel_facts(ring1_unsealed, uses_builtin, 41).  rel_bytes(ring1_unsealed, uses_builtin, 1530).
  rel_facts(ring1_unsealed, writes_to, 140).  rel_bytes(ring1_unsealed, writes_to, 4760).

-- ring1_image: charclass + ring1 AS SHIPPED (sealed), what parseFile forks per clause
program(ring1_image).  store_facts(ring1_image, 1994).  store_bytes(ring1_image, 106418).
  bucket_facts(ring1_image, world, 92).  bucket_bytes(ring1_image, world, 1831).
  bucket_facts(ring1_image, executable, 675).  bucket_bytes(ring1_image, executable, 54210).
  bucket_facts(ring1_image, about_rule, 974).  bucket_bytes(ring1_image, about_rule, 34828).
  bucket_facts(ring1_image, about_fact, 188).  bucket_bytes(ring1_image, about_fact, 13664).
  bucket_facts(ring1_image, provenance, 0).  bucket_bytes(ring1_image, provenance, 0).
  bucket_facts(ring1_image, kernel_table, 65).  bucket_bytes(ring1_image, kernel_table, 1885).
  rel_facts(ring1_image, asserted_by, 94).  rel_bytes(ring1_image, asserted_by, 6785).
  rel_facts(ring1_image, concludes, 140).  rel_bytes(ring1_image, concludes, 4990).
  rel_facts(ring1_image, conclusion_lit, 140).  rel_bytes(ring1_image, conclusion_lit, 14215).
  rel_facts(ring1_image, conclusion_tense, 140).  rel_bytes(ring1_image, conclusion_tense, 5600).
  rel_facts(ring1_image, has_premise, 395).  rel_bytes(ring1_image, has_premise, 13037).
  rel_facts(ring1_image, in_perspective, 94).  rel_bytes(ring1_image, in_perspective, 6879).
  rel_facts(ring1_image, premise_lit, 395).  rel_bytes(ring1_image, premise_lit, 36635).
  rel_facts(ring1_image, premise_neg, 33).  rel_bytes(ring1_image, premise_neg, 1317).
  rel_facts(ring1_image, premise_pos, 266).  rel_bytes(ring1_image, premise_pos, 9884).
  rel_facts(ring1_image, rule, 140).  rel_bytes(ring1_image, rule, 3360).

-- ring1_world: boot + charclass + ring1, what the tower audits
program(ring1_world).  store_facts(ring1_world, 2653).  store_bytes(ring1_world, 140289).
  bucket_facts(ring1_world, world, 273).  bucket_bytes(ring1_world, world, 6673).
  bucket_facts(ring1_world, executable, 758).  bucket_bytes(ring1_world, executable, 60739).
  bucket_facts(ring1_world, about_rule, 1167).  bucket_bytes(ring1_world, about_rule, 41957).
  bucket_facts(ring1_world, about_fact, 200).  bucket_bytes(ring1_world, about_fact, 14574).
  bucket_facts(ring1_world, provenance, 185).  bucket_bytes(ring1_world, provenance, 14338).
  bucket_facts(ring1_world, kernel_table, 70).  bucket_bytes(ring1_world, kernel_table, 2008).
  rel_facts(ring1_world, asserted_by, 100).  rel_bytes(ring1_world, asserted_by, 7246).
  rel_facts(ring1_world, concludes, 161).  rel_bytes(ring1_world, concludes, 5789).
  rel_facts(ring1_world, conclusion_lit, 161).  rel_bytes(ring1_world, conclusion_lit, 16191).
  rel_facts(ring1_world, conclusion_tense, 161).  rel_bytes(ring1_world, conclusion_tense, 6440).
  rel_facts(ring1_world, derived_by, 185).  rel_bytes(ring1_world, derived_by, 14338).
  rel_facts(ring1_world, has_conclusion, 21).  rel_bytes(ring1_world, has_conclusion, 756).
  rel_facts(ring1_world, has_premise, 436).  rel_bytes(ring1_world, has_premise, 14390).
  rel_facts(ring1_world, in_perspective, 100).  rel_bytes(ring1_world, in_perspective, 7328).
  rel_facts(ring1_world, premise_lit, 436).  rel_bytes(ring1_world, premise_lit, 40684).
  rel_facts(ring1_world, premise_neg, 42).  rel_bytes(ring1_world, premise_neg, 1678).
  rel_facts(ring1_world, premise_pos, 296).  rel_bytes(ring1_world, premise_pos, 11135).
  rel_facts(ring1_world, reads_from, 27).  rel_bytes(ring1_world, reads_from, 972).
  rel_facts(ring1_world, rule, 161).  rel_bytes(ring1_world, rule, 3864).
  rel_facts(ring1_world, uses_builtin, 2).  rel_bytes(ring1_world, uses_builtin, 74).
  rel_facts(ring1_world, writes_to, 21).  rel_bytes(ring1_world, writes_to, 723).

-- spat: boot + spat + one week
program(spat).  store_facts(spat, 11380).  store_bytes(spat, 670445).
  bucket_facts(spat, world, 4350).  bucket_bytes(spat, world, 123142).
  bucket_facts(spat, executable, 736).  bucket_bytes(spat, executable, 69633).
  bucket_facts(spat, about_rule, 1609).  bucket_bytes(spat, about_rule, 57955).
  bucket_facts(spat, about_fact, 360).  bucket_bytes(spat, about_fact, 30374).
  bucket_facts(spat, provenance, 4221).  bucket_bytes(spat, provenance, 386772).
  bucket_facts(spat, kernel_table, 104).  bucket_bytes(spat, kernel_table, 2569).
  rel_facts(spat, asserted_by, 180).  rel_bytes(spat, asserted_by, 15106).
  rel_facts(spat, concludes, 152).  rel_bytes(spat, concludes, 5690).
  rel_facts(spat, conclusion_lit, 152).  rel_bytes(spat, conclusion_lit, 17541).
  rel_facts(spat, conclusion_tense, 152).  rel_bytes(spat, conclusion_tense, 6080).
  rel_facts(spat, derived_by, 4221).  rel_bytes(spat, derived_by, 386772).
  rel_facts(spat, has_conclusion, 152).  rel_bytes(spat, has_conclusion, 5472).
  rel_facts(spat, has_premise, 432).  rel_bytes(spat, has_premise, 14256).
  rel_facts(spat, in_perspective, 180).  rel_bytes(spat, in_perspective, 15268).
  rel_facts(spat, premise_lit, 432).  rel_bytes(spat, premise_lit, 48444).
  rel_facts(spat, premise_neg, 59).  rel_bytes(spat, premise_neg, 2319).
  rel_facts(spat, premise_pos, 276).  rel_bytes(spat, premise_pos, 10613).
  rel_facts(spat, reads_from, 158).  rel_bytes(spat, reads_from, 5557).
  rel_facts(spat, rule, 152).  rel_bytes(spat, rule, 3648).
  rel_facts(spat, uses_builtin, 76).  rel_bytes(spat, uses_builtin, 2791).
  rel_facts(spat, writes_to, 152).  rel_bytes(spat, writes_to, 5177).

-- goof: boot + goof, nine geometries in nine ledgers
program(goof).  store_facts(goof, 4742).  store_bytes(goof, 285778).
  bucket_facts(goof, world, 1834).  bucket_bytes(goof, world, 59661).
  bucket_facts(goof, executable, 201).  bucket_bytes(goof, executable, 15639).
  bucket_facts(goof, about_rule, 464).  bucket_bytes(goof, about_rule, 17091).
  bucket_facts(goof, about_fact, 450).  bucket_bytes(goof, about_fact, 39099).
  bucket_facts(goof, provenance, 1690).  bucket_bytes(goof, provenance, 151354).
  bucket_facts(goof, kernel_table, 103).  bucket_bytes(goof, kernel_table, 2934).
  rel_facts(goof, asserted_by, 225).  rel_bytes(goof, asserted_by, 19451).
  rel_facts(goof, concludes, 49).  rel_bytes(goof, concludes, 1862).
  rel_facts(goof, conclusion_lit, 49).  rel_bytes(goof, conclusion_lit, 4686).
  rel_facts(goof, conclusion_tense, 49).  rel_bytes(goof, conclusion_tense, 1960).
  rel_facts(goof, derived_by, 1690).  rel_bytes(goof, derived_by, 151354).
  rel_facts(goof, has_conclusion, 49).  rel_bytes(goof, has_conclusion, 1764).
  rel_facts(goof, has_premise, 103).  rel_bytes(goof, has_premise, 3399).
  rel_facts(goof, in_perspective, 225).  rel_bytes(goof, in_perspective, 19648).
  rel_facts(goof, premise_lit, 103).  rel_bytes(goof, premise_lit, 9777).
  rel_facts(goof, premise_neg, 10).  rel_bytes(goof, premise_neg, 398).
  rel_facts(goof, premise_pos, 77).  rel_bytes(goof, premise_pos, 3022).
  rel_facts(goof, reads_from, 68).  rel_bytes(goof, reads_from, 2532).
  rel_facts(goof, rule, 49).  rel_bytes(goof, rule, 1176).
  rel_facts(goof, uses_builtin, 10).  rel_bytes(goof, uses_builtin, 369).
  rel_facts(goof, writes_to, 49).  rel_bytes(goof, writes_to, 1785).

-- wtf: boot + wtf
program(wtf).  store_facts(wtf, 7595).  store_bytes(wtf, 426376).
  bucket_facts(wtf, world, 2042).  bucket_bytes(wtf, world, 63535).
  bucket_facts(wtf, executable, 961).  bucket_bytes(wtf, executable, 76424).
  bucket_facts(wtf, about_rule, 2106).  bucket_bytes(wtf, about_rule, 75219).
  bucket_facts(wtf, about_fact, 672).  bucket_bytes(wtf, about_fact, 56198).
  bucket_facts(wtf, provenance, 1722).  bucket_bytes(wtf, provenance, 152618).
  bucket_facts(wtf, kernel_table, 92).  bucket_bytes(wtf, kernel_table, 2382).
  rel_facts(wtf, asserted_by, 336).  rel_bytes(wtf, asserted_by, 27940).
  rel_facts(wtf, concludes, 212).  rel_bytes(wtf, concludes, 7537).
  rel_facts(wtf, conclusion_lit, 212).  rel_bytes(wtf, conclusion_lit, 20923).
  rel_facts(wtf, conclusion_tense, 212).  rel_bytes(wtf, conclusion_tense, 8480).
  rel_facts(wtf, derived_by, 1722).  rel_bytes(wtf, derived_by, 152618).
  rel_facts(wtf, has_conclusion, 212).  rel_bytes(wtf, has_conclusion, 7632).
  rel_facts(wtf, has_premise, 537).  rel_bytes(wtf, has_premise, 17721).
  rel_facts(wtf, in_perspective, 336).  rel_bytes(wtf, in_perspective, 28258).
  rel_facts(wtf, premise_lit, 537).  rel_bytes(wtf, premise_lit, 50413).
  rel_facts(wtf, premise_neg, 55).  rel_bytes(wtf, premise_neg, 2111).
  rel_facts(wtf, premise_pos, 406).  rel_bytes(wtf, premise_pos, 15314).
  rel_facts(wtf, reads_from, 218).  rel_bytes(wtf, reads_from, 7657).
  rel_facts(wtf, rule, 212).  rel_bytes(wtf, rule, 5088).
  rel_facts(wtf, uses_builtin, 42).  rel_bytes(wtf, uses_builtin, 1550).
  rel_facts(wtf, writes_to, 212).  rel_bytes(wtf, writes_to, 7217).

-- loot: boot + loot + the wanderer
program(loot).  store_facts(loot, 1731).  store_bytes(loot, 90108).
  bucket_facts(loot, world, 271).  bucket_bytes(loot, world, 7232).
  bucket_facts(loot, executable, 240).  bucket_bytes(loot, executable, 18205).
  bucket_facts(loot, about_rule, 568).  bucket_bytes(loot, about_rule, 20940).
  bucket_facts(loot, about_fact, 367).  bucket_bytes(loot, about_fact, 26995).
  bucket_facts(loot, provenance, 163).  bucket_bytes(loot, provenance, 13316).
  bucket_facts(loot, kernel_table, 122).  bucket_bytes(loot, kernel_table, 3420).
  rel_facts(loot, asserted_by, 184).  rel_bytes(loot, asserted_by, 13388).
  rel_facts(loot, concludes, 58).  rel_bytes(loot, concludes, 2195).
  rel_facts(loot, conclusion_lit, 58).  rel_bytes(loot, conclusion_lit, 5254).
  rel_facts(loot, conclusion_tense, 58).  rel_bytes(loot, conclusion_tense, 2320).
  rel_facts(loot, derived_by, 163).  rel_bytes(loot, derived_by, 13316).
  rel_facts(loot, has_conclusion, 58).  rel_bytes(loot, has_conclusion, 2088).
  rel_facts(loot, has_premise, 124).  rel_bytes(loot, has_premise, 4092).
  rel_facts(loot, in_perspective, 183).  rel_bytes(loot, in_perspective, 13607).
  rel_facts(loot, premise_lit, 124).  rel_bytes(loot, premise_lit, 11559).
  rel_facts(loot, premise_neg, 28).  rel_bytes(loot, premise_neg, 1126).
  rel_facts(loot, premise_pos, 91).  rel_bytes(loot, premise_pos, 3664).
  rel_facts(loot, reads_from, 89).  rel_bytes(loot, reads_from, 3298).
  rel_facts(loot, rule, 58).  rel_bytes(loot, rule, 1392).
  rel_facts(loot, uses_builtin, 4).  rel_bytes(loot, uses_builtin, 148).
  rel_facts(loot, writes_to, 58).  rel_bytes(loot, writes_to, 2009).

-- cram: boot + cram + flight_log
program(cram).  store_facts(cram, 1644).  store_bytes(cram, 91315).
  bucket_facts(cram, world, 402).  bucket_bytes(cram, world, 14043).
  bucket_facts(cram, executable, 183).  bucket_bytes(cram, executable, 13413).
  bucket_facts(cram, about_rule, 421).  bucket_bytes(cram, about_rule, 15313).
  bucket_facts(cram, about_fact, 290).  bucket_bytes(cram, about_fact, 21179).
  bucket_facts(cram, provenance, 270).  bucket_bytes(cram, provenance, 25206).
  bucket_facts(cram, kernel_table, 78).  bucket_bytes(cram, kernel_table, 2161).
  rel_facts(cram, asserted_by, 145).  rel_bytes(cram, asserted_by, 10665).
  rel_facts(cram, concludes, 49).  rel_bytes(cram, concludes, 1821).
  rel_facts(cram, conclusion_lit, 49).  rel_bytes(cram, conclusion_lit, 4411).
  rel_facts(cram, conclusion_tense, 49).  rel_bytes(cram, conclusion_tense, 1974).
  rel_facts(cram, derived_by, 270).  rel_bytes(cram, derived_by, 25206).
  rel_facts(cram, has_conclusion, 49).  rel_bytes(cram, has_conclusion, 1764).
  rel_facts(cram, has_premise, 85).  rel_bytes(cram, has_premise, 2805).
  rel_facts(cram, in_perspective, 145).  rel_bytes(cram, in_perspective, 10514).
  rel_facts(cram, premise_lit, 85).  rel_bytes(cram, premise_lit, 7826).
  rel_facts(cram, premise_neg, 16).  rel_bytes(cram, premise_neg, 628).
  rel_facts(cram, premise_pos, 64).  rel_bytes(cram, premise_pos, 2562).
  rel_facts(cram, reads_from, 55).  rel_bytes(cram, reads_from, 1928).
  rel_facts(cram, rule, 49).  rel_bytes(cram, rule, 1176).
  rel_facts(cram, uses_builtin, 5).  rel_bytes(cram, uses_builtin, 184).
  rel_facts(cram, writes_to, 49).  rel_bytes(cram, writes_to, 1647).

-- the bucket a relation belongs to, so the rules need no list of their own
in_bucket(asserted_by, about_fact).
in_bucket(authority, kernel_table).
in_bucket(bridge_decl, about_rule).
in_bucket(concludes, about_rule).
in_bucket(conclusion_lit, executable).
in_bucket(conclusion_tense, about_rule).
in_bucket(derived_by, provenance).
in_bucket(edb, kernel_table).
in_bucket(has_conclusion, about_rule).
in_bucket(has_premise, about_rule).
in_bucket(hole, kernel_table).
in_bucket(in_perspective, about_fact).
in_bucket(mode, kernel_table).
in_bucket(premise_lit, executable).
in_bucket(premise_neg, about_rule).
in_bucket(premise_pos, about_rule).
in_bucket(reads_from, about_rule).
in_bucket(reserved, kernel_table).
in_bucket(rule, executable).
in_bucket(uses_builtin, about_rule).
in_bucket(writes_to, about_rule).

-- ablation(World, Rel, Removed, Verdict): the row dropped and the parse
-- asked again, COLD. `nothing` is the reference arm.
ablation(ring1_unsealed, nothing, 0, reference).
ablation(ring1_unsealed, rule, 140, diverged).
ablation(ring1_unsealed, conclusion_lit, 140, diverged).
ablation(ring1_unsealed, premise_lit, 395, diverged).
ablation(ring1_unsealed, has_premise, 395, refused).
ablation(ring1_unsealed, premise_pos, 266, identical).
ablation(ring1_unsealed, premise_neg, 33, identical).
ablation(ring1_unsealed, has_conclusion, 140, identical).
ablation(ring1_unsealed, concludes, 140, identical).
ablation(ring1_unsealed, conclusion_tense, 140, identical).
ablation(ring1_unsealed, reads_from, 140, identical).
ablation(ring1_unsealed, writes_to, 140, identical).
ablation(ring1_unsealed, uses_builtin, 41, identical).
ablation(ring1_unsealed, in_perspective, 93, identical).
ablation(ring1_unsealed, asserted_by, 93, identical).
ablation(ring1_unsealed, derived_by, 0, not_live).

corpus_files(80).
corpus_unparsed("examples/ring1/l1.dense.rofl").
refl_reader("boot.rofl", has_conclusion, rule_known, pos).
refl_reader("boot.rofl", has_premise, malformed, neg).
refl_reader("boot.rofl", has_premise, malformed, pos).
refl_reader("boot.rofl", has_conclusion, malformed, neg).
refl_reader("boot.rofl", concludes, breach, pos).
refl_reader("boot.rofl", reads_from, flow, pos).
refl_reader("boot.rofl", writes_to, flow, pos).
refl_reader("boot.rofl", asserted_by, forged, pos).
refl_reader("boot.rofl", in_perspective, forged, pos).
refl_reader("boot.rofl", uses_builtin, unmoded, pos).
refl_reader("boot.rofl", premise_pos, undefined_premise, pos).
refl_reader("boot.rofl", concludes, undefined_premise, neg).
refl_reader("boot.rofl", asserted_by, unattributed, pos).
refl_reader("boot.rofl", in_perspective, unattributed, pos).
refl_reader("examples/cram/flight_log.rofl", derived_by, fired_at, pos).
refl_reader("examples/loot/loot.rofl", derived_by, fired, pos).
refl_reader("examples/loot/loot.rofl", writes_to, uninvited, pos).
refl_reader("policy.rofl", concludes, rule_reads, pos).
refl_reader("policy.rofl", premise_pos, rule_reads, pos).
refl_reader("policy.rofl", premise_neg, rule_reads, pos).
refl_reader("policy.rofl", concludes, rule_relation, pos).
refl_reader("rules/kernel-policy.rofl", concludes, blocked_head, pos).
refl_reader("rules/kernel-policy.rofl", rule, executable, pos).
refl_reader("rules/kernel-policy.rofl", premise_neg, has_neg_rule, pos).
refl_reader("rules/kernel-policy.rofl", concludes, stratum_cone, pos).
refl_reader("rules/kernel-policy.rofl", premise_pos, stratum_cone, pos).
refl_reader("rules/kernel-policy.rofl", concludes, late_rule, pos).
refl_reader("rules/kernel-policy.rofl", concludes, demand_rel, pos).
refl_reader("rules/kernel-policy.rofl", conclusion_tense, demand_rel, pos).
refl_reader("rules/kernel-policy.rofl", premise_pos, demand_rel, pos).
refl_reader("rules/strata.rofl", concludes, dep, pos).
refl_reader("rules/strata.rofl", conclusion_tense, dep, pos).
refl_reader("rules/strata.rofl", premise_pos, dep, pos).
refl_reader("rules/strata.rofl", premise_neg, dep, pos).
refl_reader("rules/strata.rofl", concludes, dep_neg, pos).
refl_reader("rules/strata.rofl", conclusion_tense, dep_neg, pos).
refl_reader("rules/strata.rofl", premise_neg, dep_neg, pos).
refl_reader("rules/strata.rofl", concludes, stratum, pos).
refl_reader("rules/strata.rofl", conclusion_tense, stratum, pos).
refl_reader("safety.rofl", premise_lit, slot, pos).
refl_reader("safety.rofl", premise_lit, binds_at, pos).
refl_reader("safety.rofl", has_premise, bound_before, pos).
refl_reader("safety.rofl", premise_lit, op_used, pos).
refl_reader("safety.rofl", premise_lit, unsafe_rule, pos).
refl_reader("safety.rofl", concludes, blocked_head, pos).
refl_reader("safety.rofl", premise_neg, has_neg_rule, pos).
refl_reader("safety.rofl", concludes, stratum_cone, pos).
refl_reader("safety.rofl", premise_pos, stratum_cone, pos).
refl_reader("safety.rofl", concludes, late_rule, pos).
refl_reader("safety.rofl", concludes, demand_rel, pos).
refl_reader("safety.rofl", conclusion_tense, demand_rel, pos).
refl_reader("safety.rofl", premise_pos, demand_rel, pos).
refl_reader("safety.rofl", premise_pos, trigger_of, pos).
refl_reader("safety.rofl", concludes, trigger_of, pos).
refl_reader("safety.rofl", conclusion_tense, trigger_of, pos).
refl_reader("safety.rofl", premise_neg, neg_relation, pos).
refl_reader("safety.rofl", premise_pos, provenance_reader, pos).
refl_reader("safety.rofl", premise_neg, provenance_reader, pos).
