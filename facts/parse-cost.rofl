-- facts/parse-cost.rofl — GENERATED by scanners/parse_cost.ts, do not edit.
--
-- Every row is a measurement of ring 1 parsing one clause. The reasoning
-- over them is rules/parse-cost.rofl; nothing here concludes anything.
-- Taken on node v22.22.2.

-- THE SAME CLAUSE, PARSED TWICE: with the derived-reuse cache and without.
cache(on, ms, 217).
cache(on, firings, 639).
cache(on, facts, 3737).
cache(off, ms, 42).
cache(off, firings, 639).
cache(off, facts, 3737).

-- ONE CLAUSE AT FIVE LENGTHS, cache off: bytes, ms, firings, store facts.
sweep(14, 23, 316, 3091).
sweep(35, 46, 805, 4069).
sweep(125, 227, 3115, 8689).
sweep(253, 765, 6979, 16417).
sweep(509, 3505, 17011, 36481).

-- THE STORE'S TWO PRIMITIVES, microseconds per call, against a relation
-- of 1000 facts and one of 40000. Rounded: a rule cannot read a float.
primitive(store_add, 1000, 934).
primitive(store_add, 40000, 1354).
primitive(rel_all, 1000, 83564).
primitive(rel_all, 40000, 6360969).
-- (nanoseconds, so the integers stay integers)

-- WHERE THE CODE CALLS THEM. Read off src/engine.ts by name, not by line:
-- `fingerprint` is the reuse plan hashing a relation, `candidates` is the
-- premise match falling back to a scan when the index declines.
call_site(fingerprint, rel_all).
call_site(candidates, rel_all).
in_phase(fingerprint, reuse_plan).
in_phase(candidates, fixpoint).
-- and WHY the index declines: `indexProbe` returns null when no argument
-- of the premise is bound, which is the leading premise of a chart rule.
declines(candidates, no_bound_argument).
